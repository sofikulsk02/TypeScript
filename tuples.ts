//Array of fixed no of elements of different types

let myTuple: [string, number] = ["hello", 42];
let [first, second] = myTuple; //destructuring
console.log(first);
console.log(second);

let myBooks: [string, number] = ["harmonium", 1230];
console.log(myBooks);

console.log(typeof myBooks);
